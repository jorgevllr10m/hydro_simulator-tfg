run:
  name: "base_run"
  output_dir: "outputs/runs"

# Simulation parameters
simulation:
  start_date: "2026-04-01"
  end_date: "2026-06-10"
  time_step_hours: 1

# Choose one from /domain
domain:
  preset: "medium"

# Choose one from /scenarios
scenario:
  name: "baseline"
