run:
  name: "base_run"
  output_dir: "outputs/runs"

# Simulation parameters
simulation:
  start_date: "2026-01-01"
  end_date: "2026-01-03"
  time_step_hours: 1

# Spatial domain to be used.
domain:
  preset: "small"

# General simulation scenario.
scenario:
  name: "baseline"